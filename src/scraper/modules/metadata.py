def tag_cleanup(raw_tags):
    for i in raw_tags:
        with f as open('tag_cleanup.csv')
        
        
        # ! divide the second column by splitting on ' / '.
        # ! Must have all but rhe last option
        
        
# ? Can I search the 'raw' column for a tag then pick the cleaned one?

# ? How can I handle the super specific tags? Like if it's got A + B + C, then this spec tag?

